{
  "name": "apdpotential",
  "version": "1.0.0",
  "description": "APD Potential Website",
  "scripts": {
    "prebuild": "mkdir -p functions",
    "build": "npm install && cd functions && npm install"
  },
  "dependencies": {
    "node-fetch": "^2.6.9"
  },
  "devDependencies": {
    "netlify-cli": "^21.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
