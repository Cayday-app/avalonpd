{
  "name": "apdpotential",
  "version": "1.0.0",
  "private": true,
  "description": "APD Potential Website",
  "scripts": {
    "prebuild": "cd netlify/functions && npm install",
    "build": "npm install"
  },
  "dependencies": {
    "node-fetch": "^2.6.9"
  },
  "devDependencies": {
    "netlify-cli": "^21.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
