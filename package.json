{
  "name": "apd-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "cd .netlify/functions && npm install"
  },
  "dependencies": {
    "node-fetch": "2.6.7"
  },
  "description": "APD Chat Application",
  "devDependencies": {
    "netlify-cli": "^21.4.2"
  }
}
